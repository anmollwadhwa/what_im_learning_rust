fn main(){
    let x: f64 = 1.1;
    let y = 2.2;
}
// no need to write the type inference, rust compiler can figure it out
//in some advanced cases, rust compiler will ask to write the type inference if it can't figure out
//but it will NEVER get it wrong. 